# Reproducible Research: Peer Assessment 1


## Loading and preprocessing the data



## What is mean total number of steps taken per day?



## What is the average daily activity pattern?



## Imputing missing values
y <- A5$steps[is.na(A5$steps)]


## Are there differences in activity patterns between weekdays and weekends?

activity1<-read.csv(activity.csv)

library(ggplot2)
library(scales)
activity$date<- as.Date(activity$date, format="%Y-%m-%d")
class(activity$date)


names(activity)
hist(activity$steps)
head(activity)
fix(activity)
cumsum(steps)

A2<-activity[,sum(step), by= "date"]
library(plyr)
A2<-ddply(activity, .(date), transform, stepsperday = sum(steps))
names(A2)
A3<-A2[,c(2,4)]
fix(A2)
hist(A3$stepsperday, xlab = "Steps Per Day")
?hist
summary(A3$stepsperday)


A5<- activity[ ,c(1,3)]
A5[is.na(A5)] <- 0


A4<-ddply(A5, .(interval), transform, stepsperday = sum(steps))
A4<-A4[,c(2,3)]
A4<-unique(A4)
plot(A4$interval,A4$stepsperday, type = "l" ,xlab =  "Interval", ylab = "Steps Per Day")
names(A4)
?plot
head(A4)


A5<- activity[ ,c(1,3)]

function (d) {


}



A5<-subset(A5, steps == "NA")



A5[is.na(A5)] <- 0

A7<-subset(A5, steps == 0)
A7<-ddply(A7, .(interval), transform, stepsperday = sum(steps))
A7<-unique(A7)

class(A5$steps)
names(A5)

